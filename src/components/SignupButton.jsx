import React from "react";
import { StyledButton } from "../styles/Button";

const SignupButton = ({ onClick }) => {
  return <StyledButton onClick={onClick}>Signup</StyledButton>;
};

export default SignupButton;
